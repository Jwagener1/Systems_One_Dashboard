"""Services package.

Services are abstractions over external resources such as databases,
web APIs or file systems.  Separating out services allows
view‑models and views to depend on clean interfaces rather than on
concrete implementations.  This improves testability and makes
it easier to swap out implementations.
"""